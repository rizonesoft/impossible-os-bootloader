## Impossible OS — GRUB early config (embedded in core.efi)
## This only sets the prefix so GRUB knows where to find modules and grub.cfg.
## The actual boot config is in src/boot/grub/grub.cfg (copied to ESP).

set prefix=(hd0,gpt1)/boot/grub
configfile $prefix/grub.cfg
