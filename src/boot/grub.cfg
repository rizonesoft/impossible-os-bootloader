# GRUB Configuration — Impossible OS (UEFI)

set timeout=3
set default=0

# Request a graphical framebuffer for the kernel
set gfxmode=1024x768x32,auto
set gfxpayload=keep

menuentry "Impossible OS" {
    multiboot2 /boot/kernel.elf
    module2 /boot/initrd.img
    boot
}
